export class Pedido {
    logistics!: { idUser: number };
    school!: { idUser: number };
    food!: { idFood: number };
    quantity!: number;
    status!: string;
    deliveryDate!: string;
    comment!: string;
}
